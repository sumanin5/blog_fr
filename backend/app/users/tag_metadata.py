# ============================================================
# Users - 用户管理
# ============================================================
USERS_TAG_METADATA = {
    "name": "users",
    "description": """
## 用户管理模块

提供完整的用户认证和管理功能。

### 核心功能

#### 🔐 认证与授权
- **JWT Token 认证**：基于 JWT 的无状态认证机制
- **OAuth2 密码模式**：标准的 OAuth2 密码授权流程
- **角色权限控制**：支持普通用户、管理员、超级管理员三级权限

#### 👤 用户管理
- **用户注册**：支持邮箱注册，自动生成唯一用户名
- **用户登录**：返回 access_token 和 refresh_token
- **个人信息管理**：查看和更新个人资料
- **密码管理**：修改密码功能

#### 🛡️ 安全特性
- **密码加密**：使用 bcrypt 加密存储
- **Token 过期机制**：access_token 30天，refresh_token 7天
- **权限验证**：基于依赖注入的权限检查

### 权限级别

| 角色 | 权限 | 说明 |
|------|------|------|
| 普通用户 | `is_active=True` | 可以创建和管理自己的文章 |
| 管理员 | `is_admin=True` | 可以管理所有文章和用户 |
| 超级管理员 | `is_superadmin=True` | 拥有所有权限，包括 GitOps 同步 |

### 典型流程

```
1. 注册 → POST /users/register
2. 登录 → POST /users/login (获取 token)
3. 访问受保护资源 → 携带 Authorization: Bearer <token>
4. Token 过期 → 使用 refresh_token 刷新
```
    """,
}
