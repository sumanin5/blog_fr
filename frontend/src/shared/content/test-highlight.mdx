# 代码高亮测试

## Python 测试

```python
def hello_world():
    print("Hello, World!")
    return 42

class MyClass:
    def __init__(self, name):
        self.name = name
```

## JavaScript 测试

```javascript
const greeting = "Hello";
function sayHello(name) {
  console.log(`${greeting}, ${name}!`);
  return true;
}
```

## TypeScript 测试

```typescript
interface User {
  id: number;
  name: string;
}

const user: User = {
  id: 1,
  name: "Alice",
};
```

## 行内代码测试

这是行内代码：`const x = 42;`

## Alert 组件测试

import { Alert } from "@/shared/components/ui/alert";

<Alert className="my-4">
  <div className="flex items-start gap-3">
    <span className="flex-shrink-0 text-2xl">💡</span>
    <div className="flex-1">
      <h4 className="mb-1 font-semibold">测试提示</h4>
      <p className="text-sm">
        这是一个测试提示组件，用来验证文字是否正常换行显示。如果你看到这段文字正常显示，说明问题已经解决了。
      </p>
    </div>
  </div>
</Alert>
