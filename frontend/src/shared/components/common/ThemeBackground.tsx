/**
 * 🎨 简化的主题背景组件
 *
 * 使用纯 CSS 实现，性能更好，代码更简洁
 */
export function ThemeBackground() {
  return (
    <div className="from-background via-background to-muted/20 pointer-events-none fixed inset-0 -z-10 bg-gradient-to-br" />
  );
}
