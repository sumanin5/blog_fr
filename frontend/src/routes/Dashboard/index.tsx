import { Route } from "react-router-dom";
import Dashboard from "@/pages/dashboard/Dashboard";

/**
 * 仪表盘模块路由
 * 路径：/dashboard
 */
export const dashboardRoutes = <Route index element={<Dashboard />} />;
