# 前端项目结构说明

## 📁 目录结构

```
frontend/
├── 📚 docs/                    # 项目文档
│   ├── README.md               # 文档索引
│   ├── setup/                  # 设置指南
│   │   └── fonts.md            # 字体配置
│   └── api/                    # API 文档
│
├── 🔧 scripts/                 # 脚本工具
│   ├── README.md               # 脚本说明
│   └── install-fonts.sh        # 字体安装脚本
│
├── 💻 src/                     # 源代码
│   ├── api/                    # API 客户端
│   ├── components/             # React 组件
│   ├── contexts/               # Context 提供者
│   │   └── ThemeContext.tsx    # 主题管理
│   ├── pages/                  # 页面组件
│   ├── main.tsx                # 应用入口
│   └── index.css               # 全局样式
│
├── 🎨 public/                  # 静态资源
│
├── 📋 templates/               # 模板文件
│   └── lumina/                 # Lumina 模板
│
├── 🐳 Docker 相关
│   ├── Dockerfile              # 生产环境
│   ├── Dockerfile.dev          # 开发环境
│   ├── nginx.conf              # Nginx 配置
│   └── nginx-backend-not-found.conf
│
├── ⚙️ 配置文件
│   ├── package.json            # 依赖管理
│   ├── vite.config.ts          # Vite 配置
│   ├── tsconfig.json           # TypeScript 配置
│   ├── eslint.config.js        # ESLint 配置
│   ├── .prettierrc             # Prettier 配置
│   └── components.json         # shadcn/ui 配置
│
└── 📄 文档
    ├── PROJECT_STRUCTURE.md    # 本文件
    └── README.md               # 项目说明（如果有）
```

---

## 📚 文档组织

### docs/ 目录

所有项目文档统一放在 `docs/` 目录下，按类型分类：

- **setup/** - 设置和配置指南
- **api/** - API 相关文档
- **guides/** - 使用指南（未来可添加）
- **architecture/** - 架构设计（未来可添加）

### 文档规范

- 使用 Markdown 格式
- 文件名使用小写字母和连字符
- 包含清晰的标题和示例
- 使用 emoji 增强可读性

---

## 🔧 脚本组织

### scripts/ 目录

所有脚本工具统一放在 `scripts/` 目录下：

- **install-fonts.sh** - 字体安装
- 未来可添加更多脚本...

### 脚本规范

- 使用 `.sh` 或 `.js` 扩展名
- 添加执行权限
- 包含清晰的注释
- 在 `scripts/README.md` 中说明用途

---

## 💻 源代码组织

### src/ 目录结构

```
src/
├── api/              # API 客户端和配置
├── components/       # 可复用的 React 组件
├── contexts/         # React Context 提供者
├── pages/            # 页面级组件
├── hooks/            # 自定义 Hooks
├── utils/            # 工具函数
├── types/            # TypeScript 类型定义
├── main.tsx          # 应用入口
└── index.css         # 全局样式
```

### 命名规范

- **组件文件** - PascalCase (如 `ThemeContext.tsx`)
- **工具文件** - camelCase (如 `formatDate.ts`)
- **样式文件** - kebab-case (如 `button.module.css`)

---

## 🎨 主题系统

### 文件位置

- **CSS 变量定义** - `src/index.css`
- **主题切换逻辑** - `src/contexts/ThemeContext.tsx`
- **字体配置** - `src/main.tsx` (导入) + `src/index.css` (使用)

### 主题特性

- ✅ 支持 dark / light / system 三种模式
- ✅ 实时监听系统主题变化
- ✅ 平滑的过渡动画
- ✅ 主题切换回调
- ✅ 本地化字体（不依赖外部 CDN）

---

## 🐳 Docker 配置

### 文件说明

- **Dockerfile** - 生产环境（Nginx + 静态文件）
- **Dockerfile.dev** - 开发环境（Vite dev server）
- **nginx.conf** - Nginx 主配置
- **nginx-backend-not-found.conf** - 后端 404 处理

### 使用方式

```bash
# 开发环境
docker compose -f docker-compose.dev.yml up

# 生产环境
docker compose up
```

---

## ⚙️ 配置文件说明

### package.json

- 依赖管理
- 脚本命令
- 项目元信息

### vite.config.ts

- Vite 构建配置
- 插件配置
- 路径别名

### tsconfig.json

- TypeScript 编译选项
- 类型检查规则
- 路径映射

### components.json

- shadcn/ui 组件配置
- 组件安装路径
- 样式配置

---

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 安装字体（可选）

```bash
bash scripts/install-fonts.sh
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 构建生产版本

```bash
npm run build
```

---

## 📖 相关文档

- [文档索引](./docs/README.md)
- [字体设置指南](./docs/setup/fonts.md)
- [脚本工具说明](./scripts/README.md)

---

## 🤝 贡献指南

### 添加新文档

1. 在 `docs/` 对应分类下创建 Markdown 文件
2. 更新 `docs/README.md` 的索引
3. 遵循文档规范

### 添加新脚本

1. 在 `scripts/` 目录创建脚本文件
2. 添加执行权限
3. 在 `scripts/README.md` 中添加说明

### 代码规范

- 遵循 ESLint 和 Prettier 配置
- 使用 TypeScript 类型
- 编写清晰的注释
- 保持代码简洁

---

## 📝 更新日志

### 2024-12-08

- ✅ 整理项目结构
- ✅ 创建 docs/ 目录
- ✅ 创建 scripts/ 目录
- ✅ 添加字体本地化配置
- ✅ 完善主题系统
