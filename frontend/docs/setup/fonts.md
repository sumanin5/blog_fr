# 字体本地化设置指南

## 🎯 为什么要本地化字体？

### ❌ 使用 Google Fonts CDN 的问题

- 需要外网访问（中国大陆可能被墙）
- 增加页面加载时间
- 依赖第三方服务的稳定性
- 隐私问题（Google 可能追踪用户）

### ✅ 本地化字体的优势

- 完全离线可用
- 加载速度更快（从本地加载）
- 不受网络限制
- 更好的隐私保护
- 构建时打包，部署后无需外网

---

## 📦 安装步骤

### 1. 安装 Inter 字体包

```bash
cd frontend
npm install @fontsource/inter
```

### 2. 字体已自动导入

字体已在 `src/main.tsx` 中导入：

```typescript
import "@fontsource/inter/300.css"; // Light
import "@fontsource/inter/400.css"; // Regular
import "@fontsource/inter/500.css"; // Medium
import "@fontsource/inter/600.css"; // Semi-bold
import "@fontsource/inter/700.css"; // Bold
```

### 3. CSS 中使用

字体已在 `src/index.css` 中配置：

```css
body {
  font-family:
    "Inter",
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;
}
```

---

## 🚀 工作原理

### 构建时

1. Vite 读取 `@fontsource/inter` 包中的字体文件
2. 将字体文件复制到 `dist/assets/` 目录
3. 生成带有正确路径的 CSS 文件

### 运行时

1. 浏览器加载 HTML
2. 加载 JavaScript 和 CSS
3. 从本地 `assets/` 目录加载字体文件
4. **完全不需要外网连接**

---

## 📊 文件大小对比

| 字体粗细        | 文件大小   |
| --------------- | ---------- |
| 300 (Light)     | ~100KB     |
| 400 (Regular)   | ~100KB     |
| 500 (Medium)    | ~100KB     |
| 600 (Semi-bold) | ~100KB     |
| 700 (Bold)      | ~100KB     |
| **总计**        | **~500KB** |

**注意：** 这些文件会被 Vite 自动压缩和优化。

---

## 🎨 添加其他字体

### 示例：添加 JetBrains Mono（代码字体）

```bash
npm install @fontsource/jetbrains-mono
```

```typescript
// main.tsx
import "@fontsource/jetbrains-mono/400.css";
import "@fontsource/jetbrains-mono/700.css";
```

```css
/* index.css */
code,
pre {
  font-family: "JetBrains Mono", "Consolas", "Monaco", monospace;
}
```

---

## 🔍 可用的字体包

访问 [Fontsource](https://fontsource.org/) 查看所有可用字体：

- Google Fonts 的所有字体
- 开源字体
- 完全免费

---

## ✅ 验证字体是否正常工作

### 方法 1：浏览器开发者工具

1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 刷新页面
4. 搜索 "inter"
5. 应该看到字体文件从本地加载（如 `inter-latin-400-normal.woff2`）

### 方法 2：检查 Computed 样式

1. 右键点击页面文字 → 检查
2. 查看 Computed 标签
3. 找到 `font-family`
4. 应该显示 `Inter`

---

## 🌐 部署到阿里云

### 构建命令

```bash
npm run build
```

### 构建产物

```
dist/
├── index.html
├── assets/
│   ├── index-abc123.js
│   ├── index-def456.css
│   ├── inter-latin-300-normal.woff2  ← 字体文件
│   ├── inter-latin-400-normal.woff2
│   ├── inter-latin-500-normal.woff2
│   ├── inter-latin-600-normal.woff2
│   └── inter-latin-700-normal.woff2
```

### 部署

将整个 `dist/` 目录上传到阿里云，字体文件会随应用一起部署。

**完全不需要外网连接！** ✅

---

## 🎯 总结

- ✅ 字体已本地化，不依赖外网
- ✅ 构建时自动打包
- ✅ 部署后完全离线可用
- ✅ 加载速度更快
- ✅ 在中国大陆完美工作
