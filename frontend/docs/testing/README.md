# 前端测试文档

本目录包含前端测试相关的所有文档。

## 📚 文档目录

| 文档                                                   | 说明                                    |
| ------------------------------------------------------ | --------------------------------------- |
| [01-testing-overview.md](./01-testing-overview.md)     | 测试概述：目的、原理和核心原则          |
| [02-testing-frameworks.md](./02-testing-frameworks.md) | 前端测试框架介绍与对比                  |
| [03-vitest-rtl-guide.md](./03-vitest-rtl-guide.md)     | Vitest + React Testing Library 详细指南 |
| [04-playwright-guide.md](./04-playwright-guide.md)     | Playwright E2E 测试详细指南             |

## 🚀 快速开始

### 运行组件/集成测试

```bash
# 运行所有测试（watch 模式）
npm test

# 运行一次（CI 模式）
npm run test:run

# 带 UI 界面运行
npm run test:ui

# 生成覆盖率报告
npm run test:coverage
```

### 运行 E2E 测试

```bash
# 先确保开发服务器正在运行
npm run dev

# 然后在另一个终端运行 E2E 测试
npm run test:e2e

# 带 UI 界面运行（推荐新手使用）
npm run test:e2e:ui

# 调试模式（逐步执行，可看到浏览器操作）
npm run test:e2e:debug
```

## 📂 测试文件结构

```
frontend/
├── src/
│   └── __tests__/              # 组件/集成测试
│       ├── setup.ts            # 测试环境配置
│       ├── example.test.tsx    # 示例测试
│       └── components/         # 组件测试（按需创建）
├── tests/
│   └── e2e/                    # Playwright E2E 测试
│       └── example.spec.ts     # 示例 E2E 测试
├── vitest.config.ts            # Vitest 配置
└── playwright.config.ts        # Playwright 配置
```

## 💡 测试命名规范

- **组件测试**: `ComponentName.test.tsx`
- **Hook 测试**: `useHookName.test.ts`
- **工具函数测试**: `utilName.test.ts`
- **E2E 测试**: `feature-name.spec.ts`

## 🎯 测试优先级建议

1. **优先测试核心业务流程**（登录、注册、文章发布等）
2. **其次测试公共组件**（Button、Input、Modal 等）
3. **最后测试边缘情况**（错误处理、空状态等）

不要追求 100% 覆盖率，专注于高价值测试。
